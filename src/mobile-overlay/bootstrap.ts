/**
 * Side-effect bootstrap for Capacitor/Android builds only.
 * Imported from src/main.android.tsx before the app tree mounts.
 */
export const bootstrapMobile = (): void => {
  // Reserved for future global hooks (e.g. fetch interception).
};
